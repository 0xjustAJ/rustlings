// functions1.rs
//
// Execute `rustlings hint functions1` or use the `hint` watch subcommand for a
// hint.

fn call_me() {}

fn main() {
    call_me();
    // Answer: a function was declared inside the main function without declaring it outside the main scope. the solution is to declare the call_me() using the fn key
    //outside the main scope
}
